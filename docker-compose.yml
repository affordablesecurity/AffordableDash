services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: locksmith_crm
      POSTGRES_USER: crm
      POSTGRES_PASSWORD: crm_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
